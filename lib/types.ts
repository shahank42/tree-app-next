export interface FeedItem {
  id: number;
  username: string;
  descripton: string;
  picUrl: string;
}

export interface User {
  id: string;
  name: string;
  bio: string;
  walletAddress: string;
}
